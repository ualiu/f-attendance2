<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Login - Felton Brushes Attendance</title>
    <link rel="stylesheet" href="/css/style.css" />
    <style>
      .login-page {
        min-height: 100vh;
        display: flex;
        align-items: center;
        justify-content: center;
        background: #f8f4f0;
        padding: 1rem;
      }

      .login-container {
        width: 100%;
        max-width: 420px;
      }

      .login-card {
        background: white;
        border-radius: 12px;
        box-shadow: 0 10px 40px rgba(0, 0, 0, 0.15);
        padding: 2.5rem;
      }

      .logo-section {
        text-align: center;
        margin-bottom: 2rem;
      }

      .logo {
        font-size: 1.75rem;
        font-weight: 700;
        color: #333;
        margin: 0 0 0.5rem 0;
      }

      .subtitle {
        color: #666;
        font-size: 0.95rem;
        margin: 0;
      }

      .alert-error {
        background: #fee;
        border: 1px solid #fcc;
        color: #c33;
        padding: 0.875rem;
        border-radius: 6px;
        margin-bottom: 1.5rem;
        font-size: 0.9rem;
      }

      .login-form {
        margin-top: 0;
      }

      .form-group {
        margin-bottom: 1.25rem;
      }

      .form-group label {
        display: block;
        font-weight: 600;
        color: #333;
        margin-bottom: 0.5rem;
        font-size: 0.9rem;
      }

      .form-group input {
        width: 100%;
        padding: 0.875rem;
        border: 1px solid #ddd;
        border-radius: 6px;
        font-size: 1rem;
        transition: border-color 0.2s;
        box-sizing: border-box;
      }

      .form-group input:focus {
        outline: none;
        border-color: #667eea;
      }

      .btn-primary {
        width: 100%;
        background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
        color: white;
        padding: 0.875rem;
        border: none;
        border-radius: 6px;
        font-size: 1rem;
        font-weight: 600;
        cursor: pointer;
        transition: transform 0.2s, box-shadow 0.2s;
      }

      .btn-primary:hover {
        transform: translateY(-1px);
        box-shadow: 0 4px 12px rgba(102, 126, 234, 0.4);
      }

      .login-footer {
        margin-top: 1.5rem;
        text-align: center;
        padding-top: 1.5rem;
        border-top: 1px solid #eee;
      }

      .login-footer p {
        margin: 0;
        color: #666;
        font-size: 0.9rem;
      }

      .login-footer a {
        color: #667eea;
        text-decoration: none;
        font-weight: 600;
      }

      .login-footer a:hover {
        text-decoration: underline;
      }

      @media (max-width: 480px) {
        .login-card {
          padding: 2rem 1.5rem;
        }

        .logo {
          font-size: 1.5rem;
        }

        .subtitle {
          font-size: 0.85rem;
        }

        .form-group input,
        .btn-primary {
          font-size: 0.95rem;
        }
      }

      @media (max-width: 360px) {
        .login-card {
          padding: 1.5rem 1rem;
        }

        .logo {
          font-size: 1.35rem;
        }
      }
    </style>
  </head>
  <body class="login-page">
    <div class="login-container">
      <div class="login-card">
        <div class="logo-section">
          <h1 class="logo">Hotline BLING</h1>
          <p class="subtitle">Attendance Management System</p>
        </div>

        <% if (error) { %>
        <div class="alert-error">
          <% if (error === 'account_disabled') { %> Your account has been
          disabled. Please contact your administrator. <% } else if (error ===
          'invalid_credentials') { %> Invalid email or password. <% } else { %>
          <%= error %> <% } %>
        </div>
        <% } %>

        <form action="/auth/login" method="POST" class="login-form">
          <div class="form-group">
            <label>Email</label>
            <input
              type="email"
              name="email"
              placeholder="you@feltonbrushes.com"
              required
              autofocus
            />
          </div>

          <div class="form-group">
            <label>Password</label>
            <input
              type="password"
              name="password"
              placeholder="Enter your password"
              required
            />
          </div>

          <button type="submit" class="btn-primary">Login</button>
        </form>

        <div class="login-footer">
          <p>
            First time setup? <a href="/create-admin">Create admin account</a>
          </p>
        </div>
      </div>
    </div>
  </body>
</html>
